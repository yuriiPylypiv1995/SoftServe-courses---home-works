# We need a function that can transform a number (integer) into a string.
# What ways of achieving this do you know?

def number_to_string(num):
    """ This function returns string from integer. """
    return str(num)

print(number_to_string(2+1))